- CLI 参数统一通过 argparse 声明，并在 main() 中用 if 分支选择性注入到 kwargs 字典再传给下游函数。
- 外部库采用 try/except ImportError + subprocess 自动 pip install 的懒加载模式，避免把可选依赖写进项目 requirements。
- 错误处理一律打印带 emoji 前缀（❌/✅/📦/📂/🏷️）的中文提示并通过 sys.exit(1) 返回非零退出码。