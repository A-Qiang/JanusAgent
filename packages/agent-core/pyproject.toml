[project]
name = "agent-core"
version = "0.1.0"
description = "JanusAgent 核心抽象层：Agent 内核、生命周期、插件接口"
readme = "README.md"
authors = [
    { name = "qiang.zeng", email = "285093074@qq.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
agent-core = "agent_core:main"

[build-system]
requires = ["uv_build>=0.11.6,<0.12.0"]
build-backend = "uv_build"
