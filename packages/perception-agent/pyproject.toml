[project]
name = "perception-agent"
version = "0.1.0"
description = "JanusAgent 多模感知层 — Core abstraction for multi-modal perception & LLM knowledge ingestion"
readme = "README.md"
authors = [
    { name = "qiang.zeng", email = "285093074@qq.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
perception-agent = "perception_agent:main"

[build-system]
requires = ["uv_build>=0.11.6,<0.12.0"]
build-backend = "uv_build"
