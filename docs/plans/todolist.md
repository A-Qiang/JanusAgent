# JanusAgent 任务清单 · TodoList

> **它和 [roadmap](roadmap.md) 的关系**：roadmap 讲「为什么 / 蓝图」，本清单讲「做什么 / 怎么勾」。
> 近期（第 1 月）拆到可直接上手的粒度，中远期保持里程碑级，随进展再细化。

**图例**：`[ ]` 待办 ｜ `[~]` 进行中 ｜ `[x]` 完成 ｜ `[!]` 阻塞
**优先级**：`P0` 必须 ｜ `P1` 重要 ｜ `P2` 可选/远期

---

## 🎯 当前聚焦（Top 3）

- [ ] `P0` 通读 `agentpool`：搞清 agent 如何定义、YAML 如何配置、如何跑起对话循环、如何接 LLM
- [ ] `P0` 让 `main.py` 用 `agentpool` 驱动一个最简 agent，替换现在的 `hello()` 占位
- [ ] `P0` 跑通一轮真实 CLI 对话（`python main.py` → 能问能答）

---

## 第 1 月 · M0 + M2：能帮我炒股的 Agent

> 两层拆解：**需求（周级）→ 任务（天级 D1~D5）**。M0 骨架精简保留（agentpool 已熟悉，pydantic-ai 底层，可扩展 tools/skill/hook）。

### M0 骨架（精简，不拆天级）
- [ ] `P0` 配置 LLM 接入：`.env` 管理 API Key（**密钥不进 git**，写进 `.gitignore` 校验）
- [ ] `P0` 规划 packages 职责边界：`agent-core`（通用内核蓄水池）vs `quant-agent`（业务）各放什么
- [ ] `P0` 写最简 quant agent YAML（基模 + system prompt）并用 `main.py` 加载，跑通多轮对话循环
- [ ] `P1` 冒烟：连续多轮对话不崩、能正常调用基模

### 需求① Week1 · 行情数据接入（让 agent 拿得到数据）
- [ ] `P0` `D1` 行情数据源选型（akshare / tushare / yfinance），定一个先接，跑通拉取样例
- [ ] `P0` `D2` 封装数据访问层：历史 K 线 / 实时行情 / 基本面，统一返回结构
- [ ] `P0` `D3` 用 pydantic-ai 把「行情查询」做成 agent 工具（含参数 schema）
- [ ] `P0` `D4` 工具挂到 quant agent，对话式验证「查某股 K 线 / 基本面」
- [ ] `P1` `D5` 数据缓存 + 异常 / 限频处理，冒烟

### 需求② Week2 · 行情分析工具（让 agent 会看盘）
- [ ] `P0` `D1` 技术指标计算（MA / MACD / RSI / KDJ 等），选库或自算
- [ ] `P0` `D2` 封装 agent 工具「技术指标分析」
- [ ] `P1` `D3` 基本面 / 财务指标解读工具（估值 / 成长 / 财务健康）
- [ ] `P0` `D4` 指标结果结构化，agent 能组织成自然语言解读
- [ ] `P0` `D5` 对话式验证「分析某股当前技术面 / 基本面」

### 需求③ Week3 · 策略定义 + 回测（让 agent 验证想法）
- [ ] `P1` `D1` 回测框架选型（backtrader / 自研简版），定方案，跑通最小示例
- [ ] `P0` `D2` 定义策略描述 schema（买卖规则 / 参数 / 标的 / 周期）
- [ ] `P0` `D3` 封装「策略定义」工具：对话式生成 / 编辑策略配置
- [ ] `P0` `D4` 封装「回测执行」工具：产出收益 / 回撤 / 胜率等指标
- [ ] `P0` `D5` 回测结果结构化 + 对话式验证「定义策略 → 回测 → 看结果」

### 需求④ Week4 · 分析建议闭环 + 验收（给出可用建议）
- [ ] `P0` `D1` 编排 system prompt / hook：串起「查行情 → 分析 → 回测 → 建议」推理流程
- [ ] `P0` `D2` 分析建议输出模板（结论 + 依据 + 风险提示）
- [ ] `P0` `D3` 风险红线 hook：拦截实盘下单类请求，强制模拟盘声明（**不接实盘**）
- [ ] `P0` `D4` **验收**：对某只股票跑一次「分析 → 建议」全流程（**仅模拟盘**）
- [ ] `P2` `D5` 本月复盘：记录协助基础质量（专属度此阶段不测）

---

## 第 2 月 · M1 + M3：懂我的定制化炒股伙伴

### M1 记忆底座 + 定制化（全局最难的地基）
- [ ] `P0` 设计记忆底座数据模型：长期记忆 + 用户画像 schema
- [ ] `P0` 用户画像采集与存储：偏好 / 资金规模 / 交易习惯 / 风控意识
- [ ] `P1` 长期记忆存取（调研能否复用 `agentpool` 的 storage）
- [ ] `P1` 一致性探针：矛盾检测 + 关键事实遗忘检测
- [ ] `P0` 画像驱动的策略 / 流程动态调整（「活的流程」）
- [ ] `P0` 记忆 / 画像**开关**，支持对照测量
- [ ] `P0` **验收**：开 / 关画像对照，协助专属度 delta > 0；一致性护栏不退化

### M3 自进化（最小闭环，别贪全）
- [ ] `P1` 成功 case 沉淀：用户认可 → 存储 → 下次自动复用
- [ ] `P2` **验收**：记录专属度 delta 是否随周上升

---

## 第 3 月 · M4：通用内核沉淀

- [ ] `P1` 把 quant 上验证的记忆 / 画像 / 自进化机制抽象进 `agent-core`
- [ ] `P1` **预留具身接口**：`perception`（感知输入）/ `actuation`（行动输出）抽象，内核模态无关
- [ ] `P2` （可选）拿一个新场景做快速冷启动，验证通用性
- [ ] `P2` **验收**：新场景冷启动成本明显低于前两个

---

## 远期 · M5+ 具身 Janus（调研态，暂挂起）

- [ ] `P2` 调研具身技术栈：VLA、多模感知、触觉传感 / 执行、机器人中间件、仿真
- [ ] `P2` 触觉方案可行性验证（**全链路最硬的一块，重点评估**）
- [ ] `P2` 具身载体形态选型（现成萌宠 / 萌玩平台，不自研硬件）

---

## 🔧 横切 · 工程基建（贯穿始终）

- [ ] `P1` 指标看板：专属度 / 一致性的记录与对照方式
- [ ] `P2` 单元测试 + CI（`pre-commit` 已配 ruff）
- [ ] `P0` 风险红线守则落文档：仅模拟盘、密钥管理、不接实盘
